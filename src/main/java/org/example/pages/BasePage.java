package org.example.pages;

public abstract class BasePage {
    /*protected WebDriver driver;
    protected Actions actions;

    protected WebDriverWait wait;

    public BasePage(WebDriver driver) {
        this.driver = driver;
        this.actions = new Actions(driver);
        wait = new WebDriverWait(driver, Duration.ofSeconds(30));
    }
    */

    /*  public String getFromProperties(String key){
        try(InputStream inputStream = new FileInputStream("C:\\Users\\bachk\\Documents\\Java_Belhard\\AutoTest\\TestNG\\src\\main\\java\\org\\example\\resourses\\drivers\\project.properties") {
            Properties prop = new Properties();
            prop.load(inputStream);
            returnValue = prop.getProperty(key);
        })
    }*/
}
